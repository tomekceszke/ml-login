server:
  port: 8888

spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tomekceszke/ml-login-config
  output:
    ansi:
      enabled: ALWAYS


